/**
 * Onboarding surface — type-only barrel.
 *
 * Phase 3 ships interfaces only; Phase 5 will add the runtime exports
 * (`export { Onboarding } from './Onboarding.tsx'`) alongside the JSX
 * implementation.
 */
export type * from './Onboarding.types.ts'
