# packages/shared — Types, Validators, Constants

## Rules
- All Zod schemas exported from `src/validators/`.
- All TypeScript types exported from `src/types/`.
- Constants exported from `src/constants/`.
- ZERO runtime dependencies except Zod.
- Every schema must have corresponding unit tests.

## Skills
@skills/zod
@skills/vitest