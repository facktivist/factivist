# Factivist S1 — C4 Architecture Diagrams

> **Phase 4 deliverable** (action plan §4.5 — `docs/architecture/s1-c4.md`).
> Three nested views of the S1 system using the
> [C4 model](https://c4model.com): **Context → Container → Component**.
> Diagrams are inline Mermaid so they render on GitHub, in IDEs, and in
> review comments without external assets.
>
> Companion files:
>
> - [`bounded-contexts.md`](./bounded-contexts.md) — per-context purpose,
>   public interface, anti-corruption boundary.
> - [`package-map.md`](./package-map.md) — bounded context ↔ `apps/*` ↔
>   `packages/*` mapping.
> - [`aggregates.md`](./aggregates.md) — DDD aggregates (owned by
>   `ddd-expert`).
> - [`threat-model.md`](./threat-model.md), [`zkp-key-custody.md`](./zkp-key-custody.md)
>   — security (owned by `sec-architect`).
>
> Source of truth for in-scope feature set: action plan §4.4 (bounded
> contexts), [`docs/product/cost-scenarios.md`](../product/cost-scenarios.md) §S1
> (8 features), [`docs/design/s1/surfaces/*.md`](../design/s1/surfaces) (9
> surfaces).

---

## LEVEL 1 — System Context

Factivist is a single product accessed from three clients (web, iOS, Android)
and integrated with five external systems. Every contributor inside the
"Factivist System" boundary is provably a unique Indian citizen (via a ZKP
nullifier set on Polygon PoS), but the system itself stores **no PII**.

```mermaid
C4Context
  title S1 — System Context

  Person(citizen, "Verified Citizen", "Holds an anoncitizen credential. Submits and comments on complaints. Identified only by an opaque handle + nullifier.")
  Person(reader, "Public Reader", "Unverified visitor. Browses, filters, and searches complaints. Cannot submit or comment.")
  Person(admin, "Admin / Moderator", "Internal operator. Reviews flagged complaints + comments. Cannot deanonymise citizens (ADR-010).")

  System_Boundary(factivist, "Factivist System (S1)") {
    System(factivist_app, "Factivist", "Web + iOS + Android. ZKP onboarding, complaint composer, public browse + search, manual moderation queue.")
  }

  System_Ext(anoncitizen, "anoncitizen Circuit", "Open-source ZKP circuit + reference verifier (forked, integrated). Generates client-side proofs; nothing of the citizen leaves the device.")
  System_Ext(polygon, "Polygon PoS Mainnet", "CitizenVerifier.sol enforces uniqueness via a nullifier set. The only on-chain artifact in S1.")
  System_Ext(supabase, "Supabase (Pro)", "Postgres (data + FTS) + Storage (photos) + Auth (admin only). India ISP mitigation via custom domain (ADR-009).")
  System_Ext(thegraph, "The Graph (Hosted)", "Free hosted indexer for CitizenVerifier nullifier events; powers verification UI without RPC calls.")
  System_Ext(cloudflare, "Cloudflare (Free)", "DNS, CDN, DDoS, WAF in front of every public endpoint.")

  Rel(citizen, factivist_app, "Verifies once, then submits + comments", "HTTPS")
  Rel(reader, factivist_app, "Browses, filters, searches", "HTTPS")
  Rel(admin, factivist_app, "Reviews moderation queue", "HTTPS (auth-gated)")

  Rel(factivist_app, anoncitizen, "Generates + verifies proofs", "snarkjs / rapidsnark in-process")
  Rel(factivist_app, polygon, "verify(proof, nullifier) + read nullifier set", "JSON-RPC")
  Rel(factivist_app, supabase, "CRUD complaints, comments, mod queue; signed-URL uploads", "Postgres + REST")
  Rel(factivist_app, thegraph, "Subscribe to nullifier-added events", "GraphQL")
  Rel(cloudflare, factivist_app, "Edge proxy + caching", "HTTPS")
```

### Context-level invariants (S1)

| # | Invariant | Enforced by |
|---|-----------|-------------|
| C-1 | No PII (name, Aadhaar, address, citizen photo) ever crosses the system boundary into Factivist storage. | ADR-010, EXIF strip server-side (ADR-004), `aidefence_has_pii` on every write path. |
| C-2 | Every write request (submit complaint, post comment, flag) is preceded by a server-side nullifier check against the Polygon `CitizenVerifier` set. | identity context (see L2). |
| C-3 | One smart contract only: `CitizenVerifier.sol`. No `ComplaintRegistry` until S2. | ADR-003. |
| C-4 | All public endpoints sit behind Supabase **custom domain** + Cloudflare proxy (India ISP block mitigation). | ADR-009. |
| C-5 | Public read access is gated by a single flag `S1_PUBLIC_BROWSE`; write access by `S1_COMPLAINT_SUBMIT` (action plan §5.4). | `feature_flags` row in Postgres. |

---

## LEVEL 2 — Container

Inside the Factivist boundary, the system is **five containers** plus the
external dependencies it integrates with. Each container has one technology
choice and one deployment target.

```mermaid
C4Container
  title S1 — Container View

  Person(citizen, "Verified Citizen")
  Person(reader, "Public Reader")
  Person(admin, "Admin / Moderator")

  System_Boundary(factivist, "Factivist System") {
    Container(web, "apps/web", "Next.js 16, React Server Components, HeroUI v3, Tailwind v4", "Public marketing + verified citizen + admin web UI. SSR by default; 'use client' islands only where needed.")
    Container(mobile, "apps/mobile", "Expo SDK, Expo Router, HeroUI Native, Uniwind (Tailwind v4)", "iOS + Android client. 100% shared business logic per ADR-008. ZKP proving on-device per ADR-011.")
    Container(api, "apps/api", "Hono on Bun runtime, deployed Fly.io BOM (primary) + SIN (failover)", "Single REST surface. Hosts identity / complaint / discovery / comment / moderation / admin routes.")
    ContainerDb(db, "packages/db", "Drizzle ORM over Supabase Postgres", "Single schema. Tables: citizens, complaints, photos, categories, constituencies, comments, moderation_queue, audit_log, feature_flags, dev_metrics.llm_calls.")
    Container(shared, "packages/shared", "TypeScript, Zod 4", "Zod schemas + types + constants. Validates client AND server (ADR-002). Zero runtime deps beyond Zod.")
    ContainerDb(storage, "Supabase Storage", "S3-compatible object storage", "Private bucket complaint-photos. Signed-URL upload + read. EXIF stripped server-side before persist.")
    Container(contracts, "packages/contracts", "Solidity 0.8.x, Hardhat", "CitizenVerifier.sol integration glue + deploy scripts + Hardhat tests against Amoy + mainnet.")
  }

  System_Ext(polygon, "Polygon PoS Mainnet", "CitizenVerifier.sol")
  System_Ext(anoncitizen, "anoncitizen Circuit", "snarkjs / rapidsnark")
  System_Ext(thegraph, "The Graph (Hosted)", "Subgraph indexing CitizenVerifier")
  System_Ext(cloudflare, "Cloudflare", "DNS, CDN, WAF")
  System_Ext(sentry, "Sentry (Free)", "Errors + scrubbed traces")

  Rel(citizen, cloudflare, "HTTPS")
  Rel(reader, cloudflare, "HTTPS")
  Rel(admin, cloudflare, "HTTPS (auth)")
  Rel(cloudflare, web, "Proxies, caches static + SSR")
  Rel(cloudflare, api, "Proxies, WAF + rate-limit")
  Rel(mobile, api, "JSON over HTTPS (Supabase custom domain)")
  Rel(web, api, "RSC fetch / TanStack Query")
  Rel(api, db, "Drizzle (the only DB access path — ADR-001)")
  Rel(api, storage, "Signed-URL grant + EXIF-stripped write")
  Rel(api, polygon, "JSON-RPC: read nullifier set")
  Rel(api, thegraph, "GraphQL: nullifier events")
  Rel(web, shared, "Zod parse + types")
  Rel(mobile, shared, "Zod parse + types")
  Rel(api, shared, "Zod parse + types")
  Rel(mobile, anoncitizen, "Generate proof on device")
  Rel(web, anoncitizen, "Generate proof in WebAssembly worker")
  Rel(api, anoncitizen, "Server-fallback proof (low-tier devices, ADR-011)")
  Rel(contracts, polygon, "Deploy + verify CitizenVerifier.sol")
  Rel(web, sentry, "JS errors (PII scrubbed via beforeSend)")
  Rel(mobile, sentry, "RN errors")
  Rel(api, sentry, "API errors + traces")
```

### Container quality attributes

| Container | Availability target | Scale (S1) | Hard constraint |
|-----------|--------------------|------------|-----------------|
| `apps/web` | 99.5% | ≤ 1k MAU | Vercel Pro region `bom1` primary |
| `apps/mobile` | n/a (client) | iOS 16+ / Android 11+ | EAS Starter; Android APK ≤ 100 MB (D2) |
| `apps/api` | 99.5% | ≤ 50 RPS sustained | Fly.io shared-cpu-1x × 1, 256 MB; `min_machines_running=1` |
| `packages/db` (Supabase) | 99.9% | ≤ 5 GB data, ≤ 20 GB storage | Supabase Pro; RLS ON; service role never client-side |
| `packages/contracts` (Polygon) | 99.95% (chain) | One verify call / onboarding | Deploy from 3/5 Safe multisig |

### Cross-container contracts

- **API request/response shapes** are defined once in `packages/shared/validators/<context>/*.ts` (Zod) and consumed by Hono on the server and by TanStack Query clients on web + mobile (ADR-002).
- **DB access** is only via Drizzle on `apps/api` (ADR-001). `apps/web` and `apps/mobile` NEVER import from `packages/db` directly — they go through `apps/api`.
- **Storage uploads** never accept raw blobs from the client. The client requests a signed URL from `apps/api`, uploads the resumable bytes (tus protocol via Supabase), and the API confirms + records the path. EXIF is stripped server-side by a Sharp pipeline triggered by a Storage webhook into a "process-photo" route in `apps/api/src/routes/complaint`.

---

## LEVEL 3 — Component (per bounded context)

One component diagram per bounded context (action plan §4.4). Each diagram
zooms into the named container(s) listed below the title and shows the
**internal modules** + their routes / UI features.

> Convention: components inside `apps/api/src/routes/<ctx>` are HTTP route
> handlers; `apps/web/src/features/<ctx>` and `apps/mobile/src/features/<ctx>`
> are UI feature folders; `packages/shared/<ctx>` holds the contract; UI
> compounds live in `packages/ui/{web,native}/src/<ctx>`.

### L3.1 — `identity/`

Owns ZKP verification + citizen credential issuance + nullifier check.

```mermaid
C4Component
  title L3 — identity context

  Person(citizen, "Citizen (un-verified or verified)")

  Container_Boundary(api_id, "apps/api/src/routes/identity") {
    Component(id_nonce, "POST /identity/nonce", "Hono handler", "Issues a one-time challenge nonce bound to the device session.")
    Component(id_verify, "POST /identity/verify", "Hono handler", "Accepts {proof, publicSignals}; calls CitizenVerifier; on success writes citizens(nullifier, state_code, district_code, created_at).")
    Component(id_me, "GET /identity/me", "Hono handler", "Returns opaque handle + verification status for the current session.")
    Component(id_check, "Nullifier guard middleware", "Hono middleware", "Mounted on every write route (complaint, comment, flag). Blocks unverified callers.")
  }

  Container_Boundary(web_id, "apps/web/src/features/identity") {
    Component(web_onboarding, "Onboarding flow", "RSC + use-client islands", "Wraps Onboarding.VerifyStep / AadhaarCapture / ProofProgress / SuccessConfirmation.")
    Component(web_prover, "wasm-prover worker", "snarkjs WASM Web Worker", "Generates Groth16 proof off the main thread.")
  }

  Container_Boundary(mob_id, "apps/mobile/src/features/identity") {
    Component(mob_onboarding, "Onboarding flow", "Expo Router screens", "Same compounds via HeroUI Native.")
    Component(mob_prover_router, "proving.ts router", "Platform.select", "iOS → rapidsnark · Android → snarkjs JS · low-tier → server fallback (ADR-011, D2).")
  }

  Container_Boundary(shared_id, "packages/shared/validators/identity + types/identity") {
    Component(shared_id_schemas, "Identity Zod schemas", "Zod 4", "Nonce challenge, proof envelope, citizen credential (PII-free).")
  }

  ContainerDb(db_citizens, "packages/db.citizens", "Drizzle table", "nullifier (pk), state_code, district_code, created_at. NO name/Aadhaar/handle.")
  Component(verifier, "CitizenVerifier.sol", "Polygon PoS", "verifyProof + addNullifier; emits NullifierAdded.")
  Component(subgraph, "The Graph subgraph", "Hosted", "Indexes NullifierAdded.")

  Rel(citizen, web_onboarding, "Starts onboarding")
  Rel(citizen, mob_onboarding, "Starts onboarding")
  Rel(web_onboarding, id_nonce, "POST")
  Rel(mob_onboarding, id_nonce, "POST")
  Rel(web_onboarding, web_prover, "Build proof in worker")
  Rel(mob_onboarding, mob_prover_router, "Build proof on-device or server")
  Rel(web_onboarding, id_verify, "POST proof")
  Rel(mob_onboarding, id_verify, "POST proof")
  Rel(id_verify, verifier, "JSON-RPC verifyProof")
  Rel(id_verify, db_citizens, "INSERT nullifier row (no PII)")
  Rel(id_me, db_citizens, "SELECT by session nullifier")
  Rel(id_check, db_citizens, "SELECT nullifier exists")
  Rel(web_onboarding, subgraph, "Subscribe to NullifierAdded (optional)")
  Rel(shared_id_schemas, web_onboarding, "Parse responses")
  Rel(shared_id_schemas, mob_onboarding, "Parse responses")
  Rel(shared_id_schemas, id_verify, "Parse requests")
```

### L3.2 — `complaint/`

Owns composition, persistence, retrieval of complaints + their photos.

```mermaid
C4Component
  title L3 — complaint context

  Person(citizen, "Verified Citizen")
  Person(reader, "Public Reader")

  Container_Boundary(api_c, "apps/api/src/routes/complaint") {
    Component(c_create, "POST /complaints", "Hono handler", "Validates Zod, calls nullifier guard, persists row, returns id.")
    Component(c_get, "GET /complaints/:id", "Hono handler", "Public read; respects S1_PUBLIC_BROWSE flag.")
    Component(c_upload, "POST /complaints/:id/photos/grant", "Hono handler", "Issues a signed Supabase Storage URL (≤ 3 photos).")
    Component(c_processed, "POST /complaints/photos/processed", "Hono webhook handler", "Receives Storage webhook after EXIF strip; flips photo row to 'ready'.")
    Component(c_exif, "exif-strip pipeline", "Sharp", "Server-side strip of EXIF + GPS + thumbnail tags.")
  }

  Container_Boundary(web_c, "apps/web/src/features/complaint") {
    Component(web_composer, "Composer surface (RSC + form)", "Next 16 RSC", "Wraps Complaint.Composer / PhotoTray / CategoryPicker / ConstituencyPicker / SubmitBar.")
    Component(web_detail, "Detail surface", "RSC", "Wraps Complaint.Card / PhotoGallery / FlagAction; embeds Comment.Thread.")
  }
  Container_Boundary(mob_c, "apps/mobile/src/features/complaint") {
    Component(mob_composer, "Composer screen", "Expo Router", "Camera + photo picker + tus-resumable upload.")
    Component(mob_detail, "Detail screen", "Expo Router", "HeroUI Native variants of the same compounds.")
  }

  Container_Boundary(shared_c, "packages/shared/validators/complaint") {
    Component(shared_c_schemas, "Complaint Zod schemas", "Zod 4", "CreateComplaintRequest, ComplaintResponse, PhotoEnvelope, FlagReason (incl. pii-leak per D4).")
  }

  ContainerDb(db_complaints, "packages/db.complaints", "Drizzle table", "id, author_nullifier_fk, category_slug, constituency_id, body, fts_vector, status, created_at.")
  ContainerDb(db_photos, "packages/db.photos", "Drizzle table", "id, complaint_id, storage_path, processed_at, sha256.")
  ContainerDb(storage_bucket, "supabase storage / complaint-photos", "Private bucket", "Signed-URL upload only; EXIF stripped before processed_at set.")

  Rel(citizen, web_composer, "Compose complaint")
  Rel(citizen, mob_composer, "Compose complaint")
  Rel(reader, web_detail, "Read")
  Rel(reader, mob_detail, "Read")
  Rel(web_composer, c_create, "POST")
  Rel(mob_composer, c_create, "POST")
  Rel(c_create, db_complaints, "INSERT")
  Rel(web_composer, c_upload, "Request signed URL")
  Rel(mob_composer, c_upload, "Request signed URL")
  Rel(c_upload, storage_bucket, "Issue signed URL")
  Rel(storage_bucket, c_processed, "Webhook on upload complete")
  Rel(c_processed, c_exif, "Strip EXIF/GPS")
  Rel(c_exif, storage_bucket, "Re-write stripped object")
  Rel(c_processed, db_photos, "UPDATE processed_at + sha256")
  Rel(web_detail, c_get, "GET")
  Rel(mob_detail, c_get, "GET")
  Rel(shared_c_schemas, web_composer, "Parse")
  Rel(shared_c_schemas, mob_composer, "Parse")
  Rel(shared_c_schemas, c_create, "Parse")
```

### L3.3 — `moderation/`

Manual queue. No Redis/Bull (ADR-006). Operator can act; cannot deanonymise.

```mermaid
C4Component
  title L3 — moderation context

  Person(admin, "Admin / Moderator")

  Container_Boundary(api_m, "apps/api/src/routes/moderation") {
    Component(m_enqueue, "POST /moderation/flags (internal)", "Hono handler", "Called by complaint / comment FlagAction. Enqueues row.")
    Component(m_list, "GET /moderation/queue", "Hono handler (admin)", "Cursor-paged queue; filters by reason incl. pii-leak (D4).")
    Component(m_decide, "POST /moderation/queue/:id/decide", "Hono handler (admin)", "Decision: keep | remove | shadow. Writes audit_log.")
    Component(m_audit_mw, "audit-log middleware", "Hono middleware", "Wraps every admin decision route; writes immutable audit row.")
  }

  Container_Boundary(web_m, "apps/web/src/features/admin") {
    Component(web_queue, "Moderation queue surface", "RSC (auth-gated)", "Surface 07. Compounds: Moderation.QueueRow / DecisionBar / ReasonBadge.")
  }

  Container_Boundary(shared_m, "packages/shared/validators/moderation") {
    Component(shared_m_schemas, "Moderation Zod schemas", "Zod 4", "FlagReason enum, DecisionInput, QueueRow.")
  }

  ContainerDb(db_modq, "packages/db.moderation_queue", "Drizzle table", "id, target_kind (complaint|comment), target_id, reason, reporter_nullifier_fk, status, decided_by, decided_at.")
  ContainerDb(db_audit, "packages/db.audit_log", "Drizzle table", "id, actor, action, target_kind, target_id, payload_hash, ts. Append-only.")

  Rel(admin, web_queue, "Reviews queue")
  Rel(web_queue, m_list, "GET")
  Rel(web_queue, m_decide, "POST decision")
  Rel(m_enqueue, db_modq, "INSERT")
  Rel(m_list, db_modq, "SELECT")
  Rel(m_decide, db_modq, "UPDATE status")
  Rel(m_decide, m_audit_mw, "Wraps")
  Rel(m_audit_mw, db_audit, "INSERT (immutable)")
  Rel(shared_m_schemas, web_queue, "Parse")
  Rel(shared_m_schemas, m_list, "Parse")
  Rel(shared_m_schemas, m_decide, "Parse")
```

### L3.4 — `discovery/`

Browse + filter + Postgres FTS (ADR-005). No external search index in S1.

```mermaid
C4Component
  title L3 — discovery context

  Person(reader, "Public Reader")

  Container_Boundary(api_d, "apps/api/src/routes/discovery") {
    Component(d_browse, "GET /discovery/browse", "Hono handler", "Filter by state / district / constituency / category. Cursor pagination.")
    Component(d_search, "GET /discovery/search", "Hono handler", "tsvector + GIN index over complaints.body + title. Plainto_tsquery.")
    Component(d_facets, "GET /discovery/facets", "Hono handler", "Returns counts per category + constituency for the active filter.")
  }

  Container_Boundary(web_d, "apps/web/src/features/discovery") {
    Component(web_browse, "Browse surface", "RSC + use-client filter", "Surface 04. Filter.ConstituencyCombobox + Filter.CategoryChips (D1).")
    Component(web_search, "Search surface", "RSC", "Surface 05. Search.Box + Result list of Complaint.Card.")
  }
  Container_Boundary(mob_d, "apps/mobile/src/features/discovery") {
    Component(mob_browse, "Browse screen", "Expo Router (tab)", "HeroUI Native equivalents.")
    Component(mob_search, "Search screen", "Expo Router (tab)", "Same.")
  }

  Container_Boundary(shared_d, "packages/shared/validators/discovery") {
    Component(shared_d_schemas, "Discovery Zod schemas", "Zod 4", "BrowseQuery, SearchQuery, FacetResponse.")
  }

  ContainerDb(db_complaints_idx, "packages/db.complaints (GIN tsvector)", "Drizzle index", "Read-only from discovery's POV — owned by complaint context.")
  ContainerDb(db_geo, "packages/db.constituencies", "Drizzle (read-only)", "Closed reference dataset (ADR-007). geo context owns it.")

  Rel(reader, web_browse, "Filter")
  Rel(reader, mob_browse, "Filter")
  Rel(reader, web_search, "Search")
  Rel(reader, mob_search, "Search")
  Rel(web_browse, d_browse, "GET")
  Rel(mob_browse, d_browse, "GET")
  Rel(web_search, d_search, "GET")
  Rel(mob_search, d_search, "GET")
  Rel(d_browse, db_complaints_idx, "SELECT")
  Rel(d_search, db_complaints_idx, "SELECT WHERE @@ tsquery")
  Rel(d_browse, db_geo, "JOIN reference")
  Rel(d_facets, db_complaints_idx, "GROUP BY")
  Rel(shared_d_schemas, web_browse, "Parse")
  Rel(shared_d_schemas, mob_browse, "Parse")
  Rel(shared_d_schemas, d_browse, "Parse")
```

### L3.5 — `geo/`

Closed reference dataset (state → district → PC → AC). Loaded via Drizzle
migration seed (ADR-007). Read-only from every other context.

```mermaid
C4Component
  title L3 — geo context

  Container_Boundary(api_g, "apps/api/src/routes/geo (read-only)") {
    Component(g_states, "GET /geo/states", "Hono handler", "List of 28 states + 8 UTs.")
    Component(g_districts, "GET /geo/states/:code/districts", "Hono handler", "Cascade.")
    Component(g_constituencies, "GET /geo/districts/:code/constituencies", "Hono handler", "Cascade.")
    Component(g_pin_lookup, "GET /geo/pin/:pin", "Hono handler", "PIN → likely constituency (best-effort, S1 returns multiple matches).")
  }

  Container_Boundary(shared_g, "packages/shared/constants/geo") {
    Component(g_codes, "geo codes + zod enums", "Zod + const", "STATE_CODES, DISTRICT_CODES (compile-time generated from seed).")
  }

  ContainerDb(db_states, "packages/db.states", "Drizzle table", "code (pk), name, type (state|ut).")
  ContainerDb(db_districts, "packages/db.districts", "Drizzle table", "code (pk), state_code, name.")
  ContainerDb(db_constituencies, "packages/db.constituencies", "Drizzle table", "code (pk), district_code, name, level (pc|ac).")
  ContainerDb(db_pin_map, "packages/db.pin_constituency", "Drizzle table", "pin, constituency_code (many-to-many).")

  Component(seed, "packages/db/seed/geo.ts", "Drizzle seed script", "Bulk-inserts closed dataset; idempotent.")

  Rel(seed, db_states, "INSERT idempotent")
  Rel(seed, db_districts, "INSERT idempotent")
  Rel(seed, db_constituencies, "INSERT idempotent")
  Rel(seed, db_pin_map, "INSERT idempotent")
  Rel(g_states, db_states, "SELECT")
  Rel(g_districts, db_districts, "SELECT WHERE state_code")
  Rel(g_constituencies, db_constituencies, "SELECT WHERE district_code")
  Rel(g_pin_lookup, db_pin_map, "SELECT JOIN constituencies")
  Rel(g_codes, g_states, "Type-narrow params")
```

### L3.6 — `comment/`

Threaded, manual-mod. Comments inherit the same nullifier guard and flag
flow as complaints.

```mermaid
C4Component
  title L3 — comment context

  Person(citizen, "Verified Citizen")
  Person(reader, "Public Reader")

  Container_Boundary(api_cm, "apps/api/src/routes/comment") {
    Component(cm_create, "POST /complaints/:id/comments", "Hono handler", "Nullifier-guarded.")
    Component(cm_list, "GET /complaints/:id/comments", "Hono handler", "Threaded by parent_id; cursor paged.")
    Component(cm_flag, "POST /comments/:id/flag", "Hono handler", "Calls moderation.m_enqueue.")
  }

  Container_Boundary(web_cm, "apps/web/src/features/comment + packages/ui/web/comment") {
    Component(web_thread, "Comment.Thread", "HeroUI v3 compound", "Renders nested rows + reply composer.")
  }
  Container_Boundary(mob_cm, "apps/mobile/src/features/comment + packages/ui/native/comment") {
    Component(mob_thread, "Comment.Thread (native)", "HeroUI Native compound", "Same domain props.")
  }

  Container_Boundary(shared_cm, "packages/shared/validators/comment") {
    Component(cm_schemas, "Comment Zod schemas", "Zod 4", "CreateCommentRequest, CommentResponse.")
  }

  ContainerDb(db_comments, "packages/db.comments", "Drizzle table", "id, complaint_id, parent_id (nullable), author_nullifier_fk, body, status, created_at.")
  Component(mod_enqueue, "moderation.POST /flags", "From moderation context", "Receives flag enqueue.")

  Rel(reader, web_thread, "Reads")
  Rel(citizen, web_thread, "Replies + flags")
  Rel(web_thread, cm_create, "POST")
  Rel(mob_thread, cm_create, "POST")
  Rel(web_thread, cm_list, "GET")
  Rel(mob_thread, cm_list, "GET")
  Rel(web_thread, cm_flag, "POST")
  Rel(mob_thread, cm_flag, "POST")
  Rel(cm_create, db_comments, "INSERT")
  Rel(cm_list, db_comments, "SELECT recursive CTE")
  Rel(cm_flag, mod_enqueue, "POST /moderation/flags (internal)")
  Rel(cm_schemas, web_thread, "Parse")
  Rel(cm_schemas, cm_create, "Parse")
```

### L3.7 — `admin/`

Operator-only shell that hosts the moderation queue + future operator tools.
Web-only in S1 (no mobile admin).

```mermaid
C4Component
  title L3 — admin context

  Person(admin, "Admin / Moderator")

  Container_Boundary(api_a, "apps/api/src/routes/admin") {
    Component(a_auth, "Admin auth guard", "Hono middleware", "Supabase Auth JWT + role=admin claim. Refuses anon/citizen tokens.")
    Component(a_flags, "GET /admin/flags", "Hono handler", "Operator view of feature_flags table (S1_PUBLIC_BROWSE, S1_COMPLAINT_SUBMIT).")
    Component(a_flags_set, "POST /admin/flags/:key", "Hono handler", "Set a flag; writes audit_log.")
    Component(a_health, "GET /admin/health", "Hono handler", "Container health + DB ping + Polygon RPC ping.")
  }

  Container_Boundary(web_a, "apps/web/src/features/admin") {
    Component(web_shell, "Admin shell layout", "Next 16 segment", "Auth-gated route group; routes to moderation, flags, health.")
    Component(web_flags, "Flags surface", "RSC", "Toggle S1_PUBLIC_BROWSE / S1_COMPLAINT_SUBMIT with confirm + audit reason.")
  }

  ContainerDb(db_flags, "packages/db.feature_flags", "Drizzle table", "key (pk), enabled, updated_by, updated_at.")
  ContainerDb(db_audit, "packages/db.audit_log", "Drizzle table", "Append-only; same one used by moderation context.")
  Component(mod_ref, "moderation routes", "Cross-context", "admin shell embeds moderation surface.")

  Rel(admin, web_shell, "Logs in")
  Rel(web_shell, a_auth, "JWT")
  Rel(web_shell, web_flags, "Render flags")
  Rel(web_flags, a_flags, "GET")
  Rel(web_flags, a_flags_set, "POST")
  Rel(a_flags, db_flags, "SELECT")
  Rel(a_flags_set, db_flags, "UPDATE")
  Rel(a_flags_set, db_audit, "INSERT")
  Rel(a_health, db_flags, "SELECT 1")
  Rel(web_shell, mod_ref, "Embeds")
```

---

## Cross-cutting concerns

| Concern | How it shows up in C4 |
|---------|----------------------|
| **Anonymity (ADR-010)** | Every L3 diagram that touches a citizen reads `author_nullifier_fk`; none reference name/Aadhaar/photo of citizen. The `dev_metrics.llm_calls` table is a separate concern (no PII either). |
| **Validation (ADR-002)** | Each L3 shows `packages/shared/validators/<ctx>` parsed by both UI and API. There is no second schema definition. |
| **DB access (ADR-001)** | Only `apps/api` containers are drawn touching `packages/db`. `apps/web` + `apps/mobile` never do. |
| **Feature flags** | `admin` context owns the `feature_flags` table; `complaint` + `discovery` route handlers read it via shared util in `apps/api/src/lib/flags.ts`. |
| **Observability** | All three runtime containers ship errors to Sentry (free) with `beforeSend` PII scrub. |
| **Auth surfaces** | Citizens are not "logged in" in the OAuth sense — they prove a nullifier per session via identity context. Admins use Supabase Auth (JWT + role). The two are separate trust roots. |

---

## What's deliberately NOT in S1 C4

- **`ComplaintRegistry.sol`** — S2 only (per ADR-003 + cost-scenarios §S2).
- **IPFS / Arweave pinning** — S2 (ADR-004).
- **Meilisearch / OpenSearch** — S3 (ADR-005).
- **LLM moderation** — S2; manual queue only in S1.
- **Public mobile admin surface** — explicitly out of scope; admins use web.
- **Multi-region active-active** — `bom` primary + `sin` failover only on the API; DB is single-region Supabase.
