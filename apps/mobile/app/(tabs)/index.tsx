import { DiscoveryScreen } from '../../src/features/discovery/DiscoveryScreen.tsx'

/**
 * Tab 1 — Home / Browse.
 *
 * Renders the discovery feed. Per design doc this is the default tab,
 * matching web's landing route.
 */
export default function HomeTab() {
  return <DiscoveryScreen />
}
